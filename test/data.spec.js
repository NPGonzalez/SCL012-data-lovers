// importamos la función `example`
import * as modulo from '../src/data.js';

describe('modulo.filterDataHouse', () => {
  it('debería ser una función', () => {
    expect(typeof modulo.filterDataHouse).toBe('function');
  });

  describe('modulo.filterDataHouse', () => {
    it('Debería retornar Cho Chang, Luna Lovegood', () => {
    });
  });
});

describe('modulo.filterDataSpecies', () => {
  it('debería ser una función', () => {
    expect(typeof modulo.filterDataSpecies).toBe('function');
  });

  describe('example', () => {
    // escribe aquí tu test
  });
});

describe('modulo.filterDataGender', () => {
  it('debería ser una función', () => {
    expect(typeof modulo.filterDataGender).toBe('function');
  });

  describe('example', () => {
    // escribe aquí tu test
  });
});

describe('modulo.dataSort', () => {
  it('debería ser una función', () => {
    expect(typeof modulo.dataSort).toBe('function');
  });

  describe('example', () => {
    // escribe aquí tu test
  });
});
